include ReservasController

h1 Listing reservas

table
  thead
    tr
      th Estado
      th
      th
      th

  tbody
    - @reservas.each do |reserva|
      tr
        td = reserva.estado
        td = link_to 'Show', reserva
        td = link_to 'Edit', edit_reserva_path(reserva)
        td = link_to 'Destroy', reserva, data: {:confirm => 'Are you sure?'}, :method => :delete
        if reserva.estado == 'Reservado'
          td = button_to 'Cancelar', reserva, data: {:confirm => 'Está seguro que quiere Cancelar la Reserva?'}, @reservas.cancelar_reserva
br

= link_to 'New Reserva', new_reserva_path
